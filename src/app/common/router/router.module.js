(function () {
    'use strict';

    /**
     * @ngdoc module
     * @name common.router
     * @requires ui.router
     * @description
     *
     * The `common.router` provides support to handling `ui.router` states and transitions.
     *
     */
    angular
        .module('common.router', [
            'ui.router'
        ]);
})();
