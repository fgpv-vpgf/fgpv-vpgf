(() => {
    'use strict';

    /**
     * @ngdoc module
     * @name app.common
     * @description
     *
     * The `app.common` description.
     *
     */
    angular
        .module('app.common', []);
})();
