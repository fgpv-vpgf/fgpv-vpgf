(() => {
    'use strict';

    /**
     * @ngdoc module
     * @name app.common
     * @requires
     * @description
     *
     * The `app.common` description.
     *
     */
    angular
        .module('app.common', []);
})();
