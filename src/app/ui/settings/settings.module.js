(() => {
    'use strict';

    /**
     * @ngdoc module
     * @name app.ui.settings
     * @description
     *
     * The `app.ui.settings` module contains the settings panel.
     */
    angular
        .module('app.ui.settings', []);
})();
