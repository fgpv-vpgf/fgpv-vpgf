(() => {
    'use strict';

    /**
     * @ngdoc module
     * @name app.ui.basemap
     * @description
     *
     * The `app.ui.basemap` description.
     *
     */
    angular
        .module('app.ui.basemap', []);
})();
