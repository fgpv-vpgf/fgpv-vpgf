(function () {
    'use strict';

    /**
     * @ngdoc module
     * @name app.ui.common
     * @description
     *
     * The `app.ui.common` module contains reusable, non-element specific directives like `rv-morph` for example.
     */
    angular
        .module('app.ui.common', []);
})();
