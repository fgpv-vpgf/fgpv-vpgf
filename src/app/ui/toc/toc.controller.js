(function () {
    'use strict';

    angular
        .module('app.ui.toc')
        .controller('TOCController', TOCController);

    /* @ngInject */
    function TOCController() {
        //var self = this;

        activate();

        function activate() {

        }
    }
})();
