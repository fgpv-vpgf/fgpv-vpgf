(() => {
    'use strict';

    /**
     * @ngdoc module
     * @name app.ui.coordinates
     * @description
     *
     * The `app.ui.coordinates` module contains coordinnates section to show mouse coordinnates when mouse mouve event.
     */
    angular
        .module('app.ui.coordinates', []);
})();
