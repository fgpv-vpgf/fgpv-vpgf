(() => {
    'use strict';

    /**
     * @ngdoc module
     * @name app.ui.widgets
     * @description
     *
     * The `app.ui.widgets` module contains toolbox panel.
     */
    angular
        .module('app.ui.widgets', []);
})();
