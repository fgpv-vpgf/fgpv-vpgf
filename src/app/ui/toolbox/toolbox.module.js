(() => {
    'use strict';

    /**
     * @ngdoc module
     * @name app.ui.toolbox
     * @description
     *
     * The `app.ui.toolbox` module contains toolbox panel.
     */
    angular
        .module('app.ui.toolbox', []);
})();
