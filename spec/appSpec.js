/* jshint jasmine: true */
/*'use strict';
var File = require('vinyl');
var es = require('event-stream');
var gapi = require('../');

describe("Print URL",function () {
    // set up spy -- spies are attached to window (special mock)
    // chaining spy with and.returnValue, all calls to the function will return a specific value
    beforeEach(function() {
      var greyMap = gapi.initAll();

      spyOn(greyMap, 'printParameters');
      spyOn(greyMap, 'printTemplate');
      spyOn(greyMap, 'printTask');

    });

    // test cases
    it('should have instantiated a base grey map', function (done) {
        // set up grey map (attach grey map to esriBundle)
        // greyMap.toHaveBeenCalled();
        // see if it returns the correct args

    });

});
*/
